# ShiftPalette 手動確認テストケース

対象コミット: `954dce9 Fix swap persistence and month clear writes`

## 前提

- 自動検証は完了済み: `npm run lint` / `npm test` / `npm run build` / `git diff --check`
- 確認対象は GitHub Pages の本番画面、または同じ commit を checkout したローカル dev server
- 実データを触る場合は、変更前のセル値をメモしてから確認する
- Excel 確認では、最新ダウンロードファイルを開き、古いファイルと取り違えない

## MV-01 ログアウト後に業務 state が残らない

目的: `useFirestoreSync` の reset により、ログアウト時に前ユーザー/前組織の勤務表が残らないことを確認する。

手順:

1. アプリを開き、職員名と勤務表が表示されることを確認する。
2. 設定メニューからログアウトする。
3. ログイン画面または未認証状態の画面に戻ることを確認する。
4. ブラウザをリロードする。
5. 可能なら別アカウントでログインする。

期待結果:

- ログアウト直後に、職員名・勤務表・備考・Excel履歴が画面に残らない。
- リロード後も旧データが一瞬表示されない。
- 別アカウントでログインした場合、旧アカウントのデータが混ざらない。

## MV-02 permission-denied 時に旧データが残らない

目的: Firestore 権限エラー時に、旧 state が残らず access denied 状態へ遷移することを確認する。

手順:

1. 正常ログイン状態で勤務表が表示されていることを確認する。
2. 権限のないアカウントでログインする、またはテスト環境で一時的に Firestore 権限を外す。
3. 画面遷移と表示内容を確認する。

期待結果:

- 権限エラー後に旧勤務表が表示され続けない。
- access denied またはログイン要求の状態になる。
- 職員名・勤務表・備考などの業務データが空になる。

## MV-03 保存失敗時に通常シフト編集がロールバックされる

目的: 楽観更新後の保存失敗で UI が保存済みのように残らないことを確認する。

手順:

1. 任意の通常職員の勤務セルを1つ選び、現在値をメモする。
2. Chrome DevTools の Network で Offline にする、または通信を切る。
3. セルのシフトを別の値に変更する。
4. エラー toast とセル表示を確認する。
5. Network を Online に戻し、リロードする。

期待結果:

- 保存失敗 toast が表示される。
- セル表示が変更前の値に戻る。
- リロード後も変更前の値のまま。
- `schedule` と `manualShifts` の片方だけが反映されたような表示にならない。

## MV-04 保存失敗時にパート時間指定編集がロールバックされる

目的: `schedule + timeRangeSchedule + manualShifts` の一括保存失敗時に UI が戻ることを確認する。

手順:

1. パート職員の時間指定セルを1つ選び、現在値をメモする。
2. Network を Offline にする。
3. 時間指定を変更する、または休暇に変更する。
4. エラー toast とセル表示を確認する。
5. Network を Online に戻し、リロードする。

期待結果:

- 保存失敗 toast が表示される。
- 時間指定、休暇表示、手動固定マーカーが変更前へ戻る。
- リロード後も変更前の状態と一致する。

## MV-05 備考保存失敗時にロールバックされる

目的: 備考も保存系共通方針に揃い、失敗時に表示と保存状態が矛盾しないことを確認する。

手順:

1. 任意の日付の備考を開き、現在値をメモする。
2. Network を Offline にする。
3. 備考を別の文字列に変更する。
4. エラー toast と備考表示を確認する。
5. Network を Online に戻し、リロードする。

期待結果:

- 保存失敗 toast が表示される。
- 備考が変更前の値に戻る。
- リロード後も変更前の値のまま。

## MV-06 月次削除失敗時に state がロールバックされる

目的: 当月初期化の失敗時に、勤務表・時間指定・固定予定・備考が消えたままにならないことを確認する。

手順:

1. 対象月に勤務表、時間指定、固定予定、備考があることを確認する。
2. Network を Offline にする。
3. 当月を白紙に戻す操作を実行する。
4. エラー toast と画面状態を確認する。
5. Network を Online に戻し、リロードする。

期待結果:

- 削除失敗 toast が表示される。
- 勤務表、時間指定、固定予定、備考が操作前の状態に戻る。
- リロード後も削除されていない。

## MV-07 パートの削除・入力結果が Excel に反映される

目的: 田村さん、藤平さんなどパート職員の画面表示と Excel 出力が一致することを確認する。

手順:

1. 田村さんまたは藤平さんの任意日付で時間指定を入力する。
2. Excel 出力を実行し、該当セルに開始時刻と終了時刻が出ていることを確認する。
3. 同じセルをクリア、または明示的に `休` にする。
4. Excel 出力を再実行する。
5. 該当セルを確認する。

期待結果:

- 時間指定入力後の Excel には、該当セルに開始/終了時刻が出る。
- クリアまたは `休` 後の Excel では、該当セルが空になる。
- 画面が `－` または `休` 相当なのに、Excel に古い時間指定が残らない。

## MV-08 Excel 出力失敗時に履歴が更新されない

目的: Excel 生成に失敗した場合に、Excel 履歴だけが成功扱いにならないことを確認する。

手順:

1. 現在の Excel 履歴表示時刻をメモする。
2. テスト環境で `exportToExcel` が失敗する状態を作る。
3. Excel 出力を実行する。
4. toast と履歴表示を確認する。

期待結果:

- Excel出力失敗 toast が表示される。
- Excel 履歴時刻が更新されない。
- ファイルが生成されていないのに「出力済み」扱いにならない。

## MV-09 自動生成を押し直すと別結果になる

目的: 再シャッフル seed により、生成済み月で再度自動生成したときに別パターンを試せることを確認する。

手順:

1. 対象月で自動生成を実行する。
2. 主要な通常職員数名のシフト並びをメモする。
3. もう一度自動生成を実行する。
4. メモしたシフト並びと比較する。
5. リロード後、さらに自動生成して seed が進み続けることを確認する。

期待結果:

- 2回目の自動生成で、少なくとも一部の通常職員の配置が変わる。
- 固定予定や手入力の保護対象は維持される。
- 保存失敗時は seed が進まず、次回だけが不自然に飛ばない。

## MV-10 固定予定が自動生成で壊れない

目的: 手入力・固定予定の保護が維持されることを確認する。

手順:

1. 任意の通常職員に `有`、`夏休`、`誕生日休`、`研`、`出`、半日休のいずれかを設定する。
2. 任意のパート職員に時間指定を設定する。
3. 自動生成を実行する。
4. 設定した固定予定と時間指定を確認する。

期待結果:

- `有`、`夏休`、`誕生日休`、`研`、`出`、半日休が自動生成で上書きされない。
- パート職員の時間指定が自動生成で上書きされない。
- 自動生成後も不足確認や集計行が破綻しない。

## MV-11 不足確認と候補検索が動く

目的: action hook 分割後も、不足確認から候補検索・配置までの導線が壊れていないことを確認する。

手順:

1. 自動生成後、不足確認を開く。
2. 不足がある日付・シフトがあれば候補検索を開く。
3. 候補者を1人選んで配置する。
4. セル表示、集計行、不足件数を確認する。

期待結果:

- 候補検索モーダルが開く。
- 候補者選択後、該当セルが更新される。
- 保存成功後に toast が出る。
- 集計行と不足表示が更新される。

## MV-12 シフト入替と取り消しが動く

目的: `useScheduleActions` への分割後も、入替と undo 保存が壊れていないことを確認する。

手順:

1. 同じ日の通常職員2名を選び、シフト入替を実行する。
2. セル表示が入れ替わることを確認する。
3. toast の取り消しを実行する。
4. セル表示を確認する。
5. リロードして保存状態を確認する。

期待結果:

- 入替後に2名のシフトが正しく交換される。
- 取り消し後に元の状態へ戻る。
- リロード後も最後に成功した状態と一致する。

## MV-13 シフト入替後の自動生成保護

目的: 入替操作が手動確定として扱われ、次回自動生成で消えないことを確認する。

手順:

1. 同じ日の通常職員2名を選び、シフト入替を実行する。
2. 入替後の2名のシフト値をメモする。
3. 自動生成を実行する。
4. 入替した日付・職員2名のセルを確認する。
5. リロードして同じ値が保存されていることを確認する。

期待結果:

- 入替後の2セルが自動生成後も維持される。
- `manualShifts` と `schedule` がずれたような挙動にならない。
- リロード後も入替後の値が残る。

## MV-14 シフト入替の保存失敗時に schedule と manualShifts が戻る

目的: 入替操作の一括保存失敗で、見た目だけ入替済みとして残らないことを確認する。

手順:

1. 同じ日の通常職員2名のシフトをメモする。
2. Network を Offline にする。
3. シフト入替を実行する。
4. エラー toast とセル表示を確認する。
5. Network を Online に戻し、リロードする。

期待結果:

- 保存失敗 toast が表示される。
- 2名のセルが入替前の値に戻る。
- リロード後も入替前の値のまま。

## MV-15 当月白紙化で職員設定が巻き戻らない

目的: `clearMonthData` が `staff` を再書き込みせず、月次削除と職員設定更新が競合しないことを確認する。

手順:

1. 別タブまたは別端末で同じアプリを開く。
2. タブAで任意の職員設定を変更して保存する。
3. タブBで当月を白紙に戻す操作を実行する。
4. タブA/B をリロードし、職員設定と当月データを確認する。

期待結果:

- 職員設定の変更が当月白紙化によって巻き戻らない。
- 当月の勤務表・時間指定・固定予定・備考は削除される。
- 職員設定、シフトパターン、祝日は残る。

## 完了判定

- MV-01 から MV-15 まで pass。
- 失敗がある場合は、対象ケース ID、操作した日付、職員名、変更前後の値、スクリーンショット、ダウンロードした Excel ファイル名を記録する。
- 重大な失敗がなければ、Claude 再レビューへ進める。
