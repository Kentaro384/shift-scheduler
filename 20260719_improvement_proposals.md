# ShiftPalette 改善提案書

作成日: 2026-07-19
作成者: Fable (Claude Code)
対象リポジトリ: `ShiftPalette`
前提資料: `ShiftPalette_設計書.md`, `20260610_audit_report.md`

このドキュメントは自己完結で書かれており、会話コンテキストなしで実装着手できることを意図している(Codex への発注プロンプトの素材として使う)。

---

## 0. 背景と目的

ShiftPalette は保育園の月次シフト作成支援アプリである。作成者の狙いは、シフト作成者(園の管理者)のストレス軽減にある。ストレスの構造は次の通り。

1. **平等の前提**: 保育士は全員平等であるべきという規範がある
2. **人手不足**: 満足にシフトを埋められず、「誰に負荷をかけるか」の選択を迫られる
3. **妥協点探索の時間**: ギリギリまで妥協点を探るため非常に時間がかかる
4. **不満**: それでも結局不満が出る

現状のアプリは「当月内の制約充足と自動生成」はよくできているが、**負荷選択(2)・妥協点探索(3)・不満の抑制(4)はほぼ人間の仕事のまま**である。以下の提案はそこを埋める。

## 0.1 現状分析(コード根拠)

- 公平性指標は**当月内**の開園/閉園回数の標準偏差のみ: `src/components/ShiftBalanceDashboard.tsx` の `calculateFairnessScore()` (約104行目)
- 生成器の候補ソートも**当月内**のパターン回数昇順: `src/lib/generator.ts` の `defaultSort` (約615行目)、`countTotalShifts()` は当月スケジュールのみ集計
- 必要人数設定は1本(`settings`)のみで、不足はすべて同じ重さの「赤」で表示される
- 生成は1発実行(Undoあり)で、複数案の比較はできない
- 時間帯カバーの評価は 30 分スロットの `1/(coverage+1)` スコア: `src/lib/generator.ts` `getCoverageScore()` (約373行目)

---

## 提案一覧(優先度順)

| 順 | 提案 | 狙うストレス | 効果 | 実装規模 |
|---|---|---|---|---|
| 1 | D-2: 手動/自動の表示差別化 + 公平性シートExcel出力 | ④不満 | 中 | 小 |
| 2 | A: 月またぎ累積の公平性(表示→生成器反映の2段階) | ②選択・④不満 | 大 | 中 |
| 3 | B: 「必須/目標」2段階の不足判定(配置基準ベース) | ②選択・③時間 | 大 | 中 |
| 4 | C: 複数案生成と比較 | ③時間 | 中 | 中〜大 |
| 5 | E: 希望休の一括入力モード | ③時間 | 中 | 小〜中 |
| 6 | F: 有給5日取得義務トラッキング | ④不満 | 小 | 小 |

---

## 提案A: 公平性を「月またぎの累積」にする

不満は「今月早番が多い」ではなく「**先月も**私だったのに」で生まれる。現状は公平性指標も生成器ソートも当月で閉じている。

### A-1: 年度累積ビュー(先行・表示のみ)

- 職員 × (開園側 / 閉園側 / 土曜出勤 / 振休) の年度累積回数を集計して表示する
- データは既に Firestore `organizations/default` の `schedule` / `timeRangeSchedule` に全月分あるため、新規収集は不要
- `ShiftBalanceDashboard` に「今月 / 年度累積」のタブ切替を追加
- 年度の起点は `settings.fiscalYear`(4月開始)
- **まず表示のみで出す理由**: 利用者が「この数字は感覚と合っているか」を検証でき、生成器へ組み込む前に指標の妥当性を確かめられる

### A-2: 生成器への反映(後続)

- `src/lib/generator.ts` の候補ソート(`defaultSort`, `sortByPatternCount`)に前月までの累積回数を第2キーとして加味する
- コンストラクタに `previousMonthsTally`(職員ID→パターン種別→回数)を渡す形にし、生成器自身は純粋関数のまま保つ
- 呼び出し側(`App.tsx` / `useScheduleActions`)で全月 `schedule` から集計して渡す

### A-3(発展): 負荷ポイント制

- 回数ではなく重み付きポイントで平準化する: 例) 土曜出勤=3pt、閉園=2pt、開園=1.5pt、標準=1pt
- 重みは園設定(`settings`)でユーザー編集可能にする
- 「早番の回数は同じでも土曜ばかりの人」の不公平を拾える
- A-1 の表示にポイント列を追加 → 妥当性確認後に A-2 のソートキーへ

### 受け入れ条件

- 年度累積ビューで全職員の累積回数が月別内訳つきで見られる
- 自動生成後、累積の偏り(最大-最小)が従来実装より拡大しない(テストで比較)
- 既存の当月内バランス指標は現状のまま残す

---

## 提案B: 不足を「必須」と「目標」の2段階にする

保育園には児童年齢別の配置基準(法定: 0歳児3:1、1・2歳児6:1、3歳児20:1、4歳以上30:1)という下限があり、園独自の「理想人数」とは別物である。現状の必要人数設定は1本しかなく、不足がすべて同じ重さで表示されるため、「どこなら妥協できるか」の判断が全部人間に残る。

### 実装スケッチ

1. `settings` にクラス別児童数(月別も可)を追加: `childrenCounts: { age0: number; age1: number; age2: number; age3plus: number }`
2. 時間帯ごとの法定必要保育士数を自動計算するユーティリティを `src/lib/` に追加(切り上げ計算、合算特例は園設定でオンオフ)
3. 不足表示を2色に分ける:
   - **基準割れ**(法定必要数未満): 赤。絶対に埋める
   - **目標割れ**(園の理想未満): 黄。妥協候補
4. `ShortageModal` を拡張し、「この日は目標-1でも基準は満たします」のような**妥協可能スロットの提示**を行う
5. 設計書14章の「時間帯カバー必要人数の園設定化(中)」はこの提案に包含される

### 受け入れ条件

- 児童数を入力すると、日別・時間帯別の法定必要数が表示される
- 不足確認で「基準割れ」と「目標割れ」が区別して表示される
- 児童数未入力の場合は従来通りの1段階表示(後方互換)

---

## 提案C: 複数案生成と比較

生成器には既に乱数が入っているため、重み(公平性優先 / カバー優先 / 土曜分散優先)を変えて複数案を生成し、比較して選ぶUIが作れる。「作っては消して」の試行錯誤を「比較して選ぶ」に変える。

### 実装スケッチ

1. 生成結果を即 Firestore に書かず、**ドラフト層**(ローカルstate)に置く
2. 3案を生成し、各案にスコアカードを表示: 不足日数 / 偏り指数 / 最大負荷者と回数
3. 1タップで採用 → 従来の `saveScheduleDates()` フローで保存(セル単位保存の原則は維持)
4. 採用前のプレビューは月次表に「ドラフト表示中」バナーを出して区別する

### 注意

- 保存APIは既存の日付単位メソッドのみ使用(全体保存APIは過去の事故により削除済み。復活させないこと)
- Undo/監査ログとの整合: 採用時に1操作として記録

---

## 提案D: 「なぜこの人なのか」を説明可能にする

不満の多くは結果ではなく**説明のなさ**から来る。

### D-1: 生成配分レポート

- 自動生成後に「今月の配分レポート」を表示: 「○○さんは早番上限到達のため△△さんに+1」「土曜は□□さんが年度累積最少のため選出」など
- 生成器が判断時に理由を `GenerationReport` として蓄積し、`generate()` の戻り値に含める(現状も警告配列を返す構造があるため拡張しやすい)

### D-2: 表示差別化 + 公平性シート(クイックウィン)

- **手動入力と自動生成の表示差別化**: `manualShifts` に情報は既にあるので、セルの隅に小マーク(例: 左上に小さな点)を表示するだけ。設計書14章の「中」優先項目
- **公平性シートのExcel出力**: 全職員の早番/遅番/土曜/振休回数の年度累積一覧を、既存のExcel出力(`src/lib/excelExport.ts`)に別シートとして追加。数字が開示されているだけで「不公平では」という詰め寄りが減る

---

## 提案E: 希望休の一括入力モード

現状はシフト作成者が全職員の希望を聞き取って1マスずつ代理入力する。

- **一括入力モード**: 職員を1人選ぶ→ミニカレンダーの日付をタップしていくだけで有給/希望休が連続入力できるUI
- 保存は既存のセル単位API(`schedule.YYYY-MM-DD.staffId` + `manualShifts`)をバッチで呼ぶ
- 本命は職員用の読み取り専用+希望休提出ビューだが、認証モデル(`allowedUsers`)の拡張が必要でスコープが大きいため別フェーズとする

---

## 提案F: 有給5日取得義務トラッキング

- 労基法の年5日有給取得義務に対応: 職員別の年度有給取得数を集計し、ペース割れを警告
- 夏休3日制限の実装(6〜8月・年度3日)と同じ仕組みが流用できる
- 表示場所は `ShiftBalanceDashboard` の年度累積ビュー(提案A-1)に相乗り

---

## 付記: 小さな修正候補

- README がプロダクト説明になっていない(設計書14章の既知項目)

## 付記: `出`/`保` の正しい意味(2026-07-19 利用者確認済み)

`出`=出勤(会議出席など)、`保`=保育、が正しい。土曜に会議がある日、保育に入るメンバーへ `保`、会議に出席するメンバーへ `出` を付けて書き分ける実運用。コードは既に対応済みで、`保` は常に、`出` は土曜のみ出勤人数に集計される(`src/types.ts` の `countsAsFullDayStaffingShift()`)。旧設計書の「出=出張・外出、保=保留・その他」という記述が誤りで、設計書・ヘルプは修正済み。改善実装時にこの意味を変えないこと。

## 付記: 実装時の共通ルール(重要)

- 月次データの保存は**日付単位または職員セル単位の専用メソッドのみ**使う。`schedule` 等のトップレベルmapを丸ごと保存するAPIは事故原因として削除済み(設計書5.9参照)
- 手動入力保護の不変条件: `manualShifts` に記録されたセルを自動処理で上書きしない
- 変更後は `npm run test`(vitest)と `npx tsc -p tsconfig.app.json --noEmit` を必ず通す
- 破壊的操作を追加する場合は直前バックアップ(`organizations/default/backups`)を必須とする
